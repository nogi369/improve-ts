export const Study = () => {
  const isFinished = true;

  return console.log({ isFinished: isFinished });
};

// Udemyの書き方は、viteプロジェクトには使えない
// export {};

// const isFinished = true;
// console.log({ isFinished });

// export default isFinished;
